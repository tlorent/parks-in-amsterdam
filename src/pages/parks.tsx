import React, { FC } from 'react';
import Layout from '../components/Layout';
import Title from '../components/atoms/Title';

const Parks: FC = () => (
    <>
        <Layout>
            <Title>Parks in Amsterdam</Title>
        </Layout>
    </>
);

export default Parks;
